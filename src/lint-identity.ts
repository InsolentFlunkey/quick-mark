// Shared by the engine and UI state without loading the Markdown parsers.
// Keep native transfer validation aligned; cross-layer tests guard this contract.
export const LINT_PROFILE_VERSION = "quickmark-2-markdownlint-0.41.1";
